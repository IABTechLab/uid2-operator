name: Build and Test
on: [pull_request, push, workflow_dispatch]

jobs:
  build:
    uses: IABTechLab/uid2-shared-actions/.github/workflows/shared-build-and-test.yaml@aaq-check-test-failures-java-build
    with:
      java_version: 21
    secrets: inherit
